---
title: "Tarea 5. Diferenciación e integración numérica."
author: "VIURQUIS NAVA DULCE MARÍA"
format: 
  html:
    grid: 
      body-width: 1000px
editor: visual
jupyter: python3
---

Importamos packages y funciones necesarias:

```{python}
#| code-fold: true

import matplotlib.pyplot as plt
import numpy as np
import math

import plotly.graph_objects as go
from scipy.differentiate import derivative
import numdifftools as nd
from scipy import integrate
import pandas as pd
from scipy import stats
```

Declaramos las funciones para los métodos de diferenciación numérica:

```{python}
def forward_diff_3pts(f, x, h):
    return (-3*f(x) + 4*f(x+h) - f(x+2*h)) / (2*h)

def backward_diff_3pts(f, x, h):
    return (3*f(x) - 4*f(x-h) + f(x-2*h)) / (2*h)

def midpoint_diff_3pts(f, x, h):
    return (f(x+h) - f(x-h)) / (2*h)

def centered_diff_5pts(f, x, h):
    return (f(x-2*h) - 8*f(x-h) + 8*f(x+h) - f(x+2*h)) / (12*h)

def endpoint_diff_5pts(f, x, h):
    return (-25*f(x) + 48*f(x+h) - 36*f(x+2*h) + 16*f(x+3*h) - 3*f(x+4*h)) / (12*h)

def centered_diff2_3pts(f, x, h):
    return (f(x-h) - 2*f(x) + f(x+h)) / (h**2)
```

Declaramos las funciones para los métodos de integración numérica:

```{python}
def trapezoidal(f, a, b):
    return (b - a) * (f(a) + f(b)) / 2

def simpson(f, a, b):
    x_0 = a
    x_2 = b
    h = (b - a) / 2
    x_1 = x_0 + h
    int_approx = (h / 3) * (f(x_0) + 4 * f(x_1) + f(x_2))
    return int_approx

def simpson_38(f, a, b):
    x_0 = a
    x_3 = b
    h = (b - a) / 3
    x_1 = x_0 + h
    x_2 = x_0 + 2 * h
    int_approx = (3 * h / 8) * (f(x_0) + 3 * f(x_1) + 3 * f(x_2) + f(x_3))
    return int_approx

def boole(f, a, b):
    x_0 = a
    x_4 = b
    h = (b - a) / 4
    x_1 = x_0 + h
    x_2 = x_0 + 2 * h
    x_3 = x_0 + 3 * h
    int_approx = (2 * h / 45) * (7 * f(x_0) + 32 * f(x_1) + 12 * f(x_2) + 32 * f(x_3) + 7 * f(x_4))
    return int_approx
```

# Ejercicio 1.

Para cada una de las siguientes funciones:

-   Realiza la respectiva gráfica en el intervalo dado.

-   Compara las gráficas de las derivadas aproximadas de la función `derivative` de `Scipy`, con dos tamaños de paso utilizando la función `nd.Derivative` (con `order =2` y `order = 4`, con las opciones de `method`: `forward`, `backward`, `central`) y la derivada *exacta* en tal intervalo.

-   Compara las gráficas de las segundas derivadas aproximadas con dos tamaños de paso utilizando la función `nd.Derivative` y la segunda derivada *exacta* en tal intervalo.

-   Realiza las gráficas de los errores absolutos en cada caso.

a)  $f(x)=e^{2x}-\cos 2x$, $x\in [0,2]$

```{python}
#| code-fold: true
#| fig-align: "center"

f_a = lambda x: np.exp(2*x) - np.cos(2*x)
df_a_exacta = lambda x: 2*np.exp(2*x) + 2*np.sin(2*x)
d2f_a_exacta = lambda x: 4*np.exp(2*x) + 4*np.cos(2*x)

x_values = np.linspace(0, 2, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f_a(x_values), label=r'$f(x) = e^{2x} - \cos(2x)$', linewidth=2)
plt.xlabel("x")
plt.ylabel("f(x)")
plt.title(r"Función $f(x) = e^{2x} - \cos(2x)$")
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

**Primera derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1

df_scipy = derivative(f_a, x_values).df

fwd3 = nd.Derivative(f_a, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_a, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_a, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_a, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_a, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_a, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_a_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.1", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_a_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_a_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_a_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_a_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_a_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_a_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_a_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25

fwd3 = nd.Derivative(f_a, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_a, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_a, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_a, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_a, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_a, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_a_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.25", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_a_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_a_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_a_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_a_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_a_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_a_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_a_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

**Segunda derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
d2cen3 = nd.Derivative(f_a, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_a_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.1", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_a_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
d2cen3 = nd.Derivative(f_a, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_a_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.25", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_a_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

b)  $f(x)=\log(x+2)-(x+1)^2$, $x\in [0,5]$

```{python}
#| code-fold: true
#| fig-align: "center"

f_b = lambda x: np.log(x + 2) - (x + 1)**2
df_b_exacta = lambda x: 1/(x + 2) - 2*(x + 1)
d2f_b_exacta = lambda x: -1/(x + 2)**2 - 2

x_values = np.linspace(0, 5, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f_b(x_values), label=r'$f(x) = \log(x+2) - (x+1)^2$', linewidth=2)
plt.xlabel("x")
plt.ylabel("f(x)")
plt.title(r"Función $f(x) = \log(x+2) - (x+1)^2$")
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

**Primera derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
df_scipy = derivative(f_b, x_values).df

fwd3 = nd.Derivative(f_b, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_b, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_b, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_b, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_b, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_b, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_b_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.1", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_b_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_b_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_b_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_b_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_b_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_b_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_b_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
fwd3 = nd.Derivative(f_b, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_b, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_b, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_b, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_b, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_b, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_b_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.25", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_b_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_b_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_b_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_b_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_b_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_b_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_b_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

**Segunda derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
d2cen3 = nd.Derivative(f_b, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_b_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.1", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_b_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
d2cen3 = nd.Derivative(f_b, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_b_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.25", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_b_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

c)  $f(x)=\sqrt{x}\,\text{sen}(x^2)$, $x\in [0,\pi]$

```{python}
#| code-fold: true
#| fig-align: "center"

f_c = lambda x: np.sqrt(x) * np.sin(x**2)
df_c_exacta = lambda x: (1/(2*np.sqrt(x)))*np.sin(x**2) + 2*x*np.sqrt(x)*np.cos(x**2)
d2f_c_exacta = lambda x: (
    -1/(4*x**(3/2))*np.sin(x**2)
    + 2*np.sqrt(x)*np.cos(x**2)
    + (1/(2*np.sqrt(x)))*2*x*np.cos(x**2)
    + 2*np.sqrt(x)*np.cos(x**2)
    - 4*x**2*np.sqrt(x)*np.sin(x**2)
    + (1/(2*np.sqrt(x)))*2*x*np.cos(x**2)
)

x_values = np.linspace(0.01, np.pi, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f_c(x_values), label=r'$f(x) = \sqrt{x}\,\sin(x^2)$', linewidth=2)
plt.xlabel("x")
plt.ylabel("f(x)")
plt.title(r"Función $f(x) = \sqrt{x}\,\sin(x^2)$")
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

**Primera derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
df_scipy = derivative(f_c, x_values).df

fwd3 = nd.Derivative(f_c, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_c, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_c, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_c, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_c, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_c, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_c_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.1", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_c_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_c_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_c_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_c_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_c_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_c_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_c_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
fwd3 = nd.Derivative(f_c, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_c, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_c, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_c, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_c, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_c, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_c_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.25", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_c_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_c_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_c_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_c_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_c_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_c_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_c_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

**Segunda derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
d2cen3 = nd.Derivative(f_c, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_c_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.1", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_c_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
d2cen3 = nd.Derivative(f_c, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_c_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.25", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_c_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

d)  $f(x)=(\cos\,3x)^2-e^{2x}$, $x\in [0,\pi/2]$

```{python}
#| code-fold: true
#| fig-align: "center"

f_d = lambda x: np.cos(3*x)**2 - np.exp(2*x)
df_d_exacta = lambda x: -6*np.cos(3*x)*np.sin(3*x) - 2*np.exp(2*x)
d2f_d_exacta = lambda x: 18*np.sin(3*x)**2 - 18*np.cos(3*x)**2 - 4*np.exp(2*x)

x_values = np.linspace(0, np.pi/2, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f_d(x_values), label=r'$f(x) = \cos^2(3x) - e^{2x}$', linewidth=2)
plt.xlabel("x")
plt.ylabel("f(x)")
plt.title(r"Función $f(x) = \cos^2(3x) - e^{2x}$")
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```

**Primera derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
df_scipy = derivative(f_d, x_values).df

fwd3 = nd.Derivative(f_d, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_d, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_d, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_d, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_d, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_d, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_d_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.1", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_d_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_d_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_d_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_d_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_d_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_d_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_d_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
fwd3 = nd.Derivative(f_d, step=h, method='forward', order=2, n=1)
bwd3 = nd.Derivative(f_d, step=h, method='backward', order=2, n=1)
mid3 = nd.Derivative(f_d, step=h, method='central', order=2, n=1)
cen5 = nd.Derivative(f_d, step=h, method='central', order=4, n=1)
fwd5 = nd.Derivative(f_d, step=h, method='forward', order=4, n=1)
bwd5 = nd.Derivative(f_d, step=h, method='backward', order=4, n=1)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=df_d_exacta(x_values), mode='lines', name="f'(x)"))
fig.add_trace(go.Scatter(x=x_values, y=df_scipy, mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=fwd3(x_values), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd3(x_values), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=mid3(x_values), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=cen5(x_values), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=fwd5(x_values), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=bwd5(x_values), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Primera derivada — h=0.25", xaxis_title="x", yaxis_title="f'(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(df_scipy - df_d_exacta(x_values)), mode='lines', name="scipy.derivative"))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd3(x_values) - df_d_exacta(x_values)), mode='lines', name='Forward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd3(x_values) - df_d_exacta(x_values)), mode='lines', name='Backward 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(mid3(x_values) - df_d_exacta(x_values)), mode='lines', name='Midpoint 3 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(cen5(x_values) - df_d_exacta(x_values)), mode='lines', name='Centered 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(fwd5(x_values) - df_d_exacta(x_values)), mode='lines', name='Forward 5 Pts'))
fig.add_trace(go.Scatter(x=x_values, y=np.abs(bwd5(x_values) - df_d_exacta(x_values)), mode='lines', name='Backward 5 Pts'))
fig.update_layout(title="Error absoluto primera derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

**Segunda derivada:**

::: panel-tabset
## h = 0.1

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.1
d2cen3 = nd.Derivative(f_d, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_d_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.1", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_d_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.1", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```

## h = 0.25

```{python}
#| code-fold: true
#| fig-align: "center"

h = 0.25
d2cen3 = nd.Derivative(f_d, step=h, method='central', order=2, n=2)

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=d2f_d_exacta(x_values), mode='lines', name="f''(x)"))
fig.add_trace(go.Scatter(x=x_values, y=d2cen3(x_values), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Segunda derivada — h=0.25", xaxis_title="x", yaxis_title="f''(x)",
                  template='plotly_white', width=800, height=500)
fig.show()
```

```{python}
#| code-fold: true
#| fig-align: "center"

fig = go.Figure()
fig.add_trace(go.Scatter(x=x_values, y=np.abs(d2cen3(x_values) - d2f_d_exacta(x_values)), mode='lines', name='Centered 3 Pts'))
fig.update_layout(title="Error absoluto segunda derivada — h=0.25", xaxis_title="x", yaxis_title="Error absoluto",
                  template='plotly_white', width=800, height=500)
fig.show()
```
:::

# Ejercicio 2

Elige 3 indicadores económicos del INEGI (del archivo `TIE_Inegi_long.xlsx`), realiza la gráfica de la serie temporal y de su derivada. Brinda un análisis de la gráfica y de la evolución del indicador a lo largo del tiempo.

Los indicadores seleccionados son: **Tipo de cambio nominal**, **Exportaciones totales** y **Tasa de desocupación urbana**.

```{python}
#| code-fold: true
#| fig-align: "center"

df_inegi = pd.read_excel("TIE_Inegi_long.xlsx", parse_dates=["Fecha"])
df_inegi = df_inegi.sort_values("Fecha").reset_index(drop=True)

indicadores = ["Tipo_cambio_nominal", "Exportaciones_totales", "Tasa_desocupacion_urbana"]
titulos = ["Tipo de cambio nominal (MXN/USD)",
           "Exportaciones totales (millones de dólares)",
           "Tasa de desocupación urbana (%)"]

for ind, titulo in zip(indicadores, titulos):
    serie = df_inegi[ind].dropna()
    fechas = df_inegi.loc[serie.index, "Fecha"]
    x_num = np.arange(len(serie))
    derivada = np.gradient(serie.values, x_num)

    fig, axes = plt.subplots(2, 1, figsize=(10, 8))
    axes[0].plot(fechas, serie.values, color='steelblue', linewidth=1.5)
    axes[0].set_title(f"Serie temporal: {titulo}")
    axes[0].set_xlabel("Fecha")
    axes[0].set_ylabel("Valor")
    axes[0].grid(alpha=0.3)

    axes[1].plot(fechas, derivada, color='firebrick', linewidth=1.2)
    axes[1].axhline(0, color='black', lw=1, ls='--')
    axes[1].set_title(f"Derivada numérica: {titulo}")
    axes[1].set_xlabel("Fecha")
    axes[1].set_ylabel("Tasa de cambio")
    axes[1].grid(alpha=0.3)

    plt.tight_layout()
    plt.show()
```

**Análisis de los indicadores:**

-   **Tipo de cambio nominal:** A lo largo del período se observa una tendencia de depreciación del peso mexicano frente al dólar. La derivada muestra episodios de alta volatilidad, especialmente alrededor de crisis económicas (1994-1995, 2008-2009 y 2020). Los períodos donde la derivada es positiva indican depreciación acelerada; donde es negativa, apreciación.

-   **Exportaciones totales:** Presentan una tendencia creciente en el largo plazo, con caídas marcadas durante la crisis financiera de 2008-2009 y la pandemia de 2020. La derivada permite identificar claramente esos puntos de quiebre donde el crecimiento se detuvo o revirtió abruptamente.

-   **Tasa de desocupación urbana:** Muestra ciclos relacionados con el desempeño económico general. Los picos de la derivada corresponden a momentos de pérdida rápida de empleos (crisis), mientras que valores negativos indican recuperación del mercado laboral.

# Ejercicio 3

Calcular el valor exacto y las aproximaciones numéricas de la siguiente integral:

$$\int_0^4 x^2e^{-x} dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a = 0
b = 4
f = lambda x: x**2 * np.exp(-x)
x_values = np.linspace(-0.3, 4.3, 100)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x) = x^2 e^{-x}$')
plt.fill_between(np.linspace(a, b, 100), f(np.linspace(a, b, 100)), alpha=0.3, color='orange', label='Área bajo la curva')
plt.title(r'Gráfica de $f(x) = x^2 e^{-x}$')
plt.xlabel('x')
plt.ylabel('f(x)')
plt.axhline(0, color='black', lw=1, ls='--')
plt.axvline(0, color='black', lw=1, ls='--')
plt.legend()
plt.grid()
plt.show()
```

El valor exacto de la integral (por partes repetida) es $2 - 26e^{-4}$:

```{python}
#| code-fold: true

integral_exacta = 2 - 26*np.exp(-4)
print(f"El valor exacto de la integral es: {integral_exacta:.8f}")
```

**Aproximación con el método trapezoidal:**

```{python}
#| code-fold: true

print("Aproximación con el método trapezoidal:")
print(trapezoidal(f, a, b))
print("Error absoluto:", abs(integral_exacta - trapezoidal(f, a, b)))
```

Utilizamos la función `integrate.trapezoid` de la biblioteca `scipy`.

```{python}
#| code-fold: true

print("Aproximación con la función `integrate.trapezoid` de `scipy`:")
x_values = np.linspace(a, b, 2)
y_values = f(x_values)
print(integrate.trapezoid(y_values, x_values))
print("Error absoluto:", abs(integral_exacta - integrate.trapezoid(y_values, x_values)))
```

**Aproximación con el método de Simpson:**

```{python}
#| code-fold: true

print("Aproximación con el método de Simpson:")
print(simpson(f, a, b))
print("Error absoluto:", abs(integral_exacta - simpson(f, a, b)))
```

Utilizamos la función `integrate.simpson` de la biblioteca `scipy`.

```{python}
#| code-fold: true

print("Aproximación con la función `integrate.simpson` de `scipy`:")
x_values = np.linspace(a, b, 3)
y_values = f(x_values)
print(integrate.simpson(y_values, x_values))
print("Error absoluto:", abs(integral_exacta - integrate.simpson(y_values, x_values)))
```

**Aproximación con el método de Simpson 3/8:**

```{python}
#| code-fold: true

print("Aproximación con el método de Simpson 3/8:")
print(simpson_38(f, a, b))
print("Error absoluto:", abs(integral_exacta - simpson_38(f, a, b)))
```

**Aproximación con el método de Boole:**

```{python}
#| code-fold: true

print("Aproximación con el método de Boole:")
print(boole(f, a, b))
print("Error absoluto:", abs(integral_exacta - boole(f, a, b)))
```

**Aproximación con la regla trapezoidal compuesta:**

```{python}
#| code-fold: true

n = 12
h = (b - a) / n
a_values = np.linspace(a, b, endpoint=False, num=n)
b_values = a_values + h
trapezoidal_compuesto = sum(trapezoidal(f, a_i, b_i) for a_i, b_i in zip(a_values, b_values))
print("Aproximación con la regla trapezoidal compuesta:")
print(trapezoidal_compuesto)
print("Error absoluto:", abs(integral_exacta - trapezoidal_compuesto))
```

Utilizamos la función `integrate.trapezoid` de la biblioteca `scipy` para la regla trapezoidal compuesta.

```{python}
#| code-fold: true

print("Aproximación con la función `integrate.trapezoid` de `scipy` para la regla trapezoidal compuesta:")
x_values = np.linspace(a, b, n+1)
y_values = f(x_values)
print(integrate.trapezoid(y_values, x_values))
print("Error absoluto:", abs(integral_exacta - integrate.trapezoid(y_values, x_values)))
```

**Aproximación con el método de Simpson compuesto:**

```{python}
#| code-fold: true

n = 12
h = (b - a) / n
a_values = np.linspace(a, b, endpoint=False, num=int(n/2))
b_values = a_values + 2*h
simpson_compuesto = sum(simpson(f, a_i, b_i) for a_i, b_i in zip(a_values, b_values))
print("Aproximación con el método de Simpson compuesto:")
print(simpson_compuesto)
print("Error absoluto:", abs(integral_exacta - simpson_compuesto))
```

Utilizamos la función `integrate.simpson` de la biblioteca `scipy` para el método de Simpson compuesto.

```{python}
#| code-fold: true

print("Aproximación con la función `integrate.simpson` de `scipy` para el método de Simpson compuesto:")
x_values = np.linspace(a, b, n+1)
y_values = f(x_values)
print(integrate.simpson(y_values, x_values))
print("Error absoluto:", abs(integral_exacta - integrate.simpson(y_values, x_values)))
```

**Aproximación con el método de Simpson 3/8 compuesto:**

```{python}
#| code-fold: true

n = 12
h = (b - a) / n
a_values = np.linspace(a, b, endpoint=False, num=int(n/3))
b_values = a_values + 3*h
simpson_38_compuesto = sum(simpson_38(f, a_i, b_i) for a_i, b_i in zip(a_values, b_values))
print("Aproximación con el método de Simpson 3/8 compuesto:")
print(simpson_38_compuesto)
print("Error absoluto:", abs(integral_exacta - simpson_38_compuesto))
```

**Aproximación con el método de Boole compuesto:**

```{python}
#| code-fold: true

n = 12
h = (b - a) / n
a_values = np.linspace(a, b, endpoint=False, num=int(n/4))
b_values = a_values + 4*h
boole_compuesto = sum(boole(f, a_i, b_i) for a_i, b_i in zip(a_values, b_values))
print("Aproximación con el método de Boole compuesto:")
print(boole_compuesto)
print("Error absoluto:", abs(integral_exacta - boole_compuesto))
```

Scipy tiene también integrado un método, `integrate.quad`, que utiliza técnicas de cuadratura adaptativa para calcular la integral de manera eficiente y precisa.

```{python}
#| code-fold: true

print("Aproximación con la función `integrate.quad` de `scipy`:")
integral_quad, error_quad = integrate.quad(f, a, b)
print(integral_quad)
print("Error absoluto:", abs(integral_exacta - integral_quad))
print("Estimación del error absoluto proporcionada por `integrate.quad`:", error_quad)
```

# Ejercicio 4

Aproximar las siguientes integrales con la función `integrate.quad` (de SciPy) y con el método de Montecarlo, en cada caso hacer una gráfica de la función e indicar el área bajo la curva.

a)  $$\int_0^1 e^{-x^2}\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a=0
b=1
f = lambda x: np.exp(- x**2)
x_values = np.linspace(-0.1, 1.1, 100)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x)$')
plt.fill_between(np.linspace(a,b,100), f(np.linspace(a,b,100)), alpha=0.3, color = '#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.axis('equal')
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo para aproximar la integral**

```{python}
#| code-fold: true 
#| fig-align: "center" 

N = 100000
ymin = 0
ymax = 1
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)
puntos_in = y_random <= f(x_random)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in], y_random[puntos_in], color='green', s=10, label='In')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=10, label='Out')
plt.plot(x_values, f(x_values), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.axis('equal')
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo = (ymax - ymin) * (b - a) * np.sum(puntos_in) / N
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

b)  $$\int_0^\pi \text{sen}(x^2)\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a=0
b=np.pi
f = lambda x: np.sin(x**2)
x_values = np.linspace(-0.1, 3.3, 100)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x)$')
plt.fill_between(np.linspace(a,b,100), f(np.linspace(a,b,100)), alpha=0.3, color = '#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.axis('equal')
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo**

```{python}
#| code-fold: true 
#| fig-align: "center" 

N = 100000
ymin = -1
ymax = 1
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y_random) <= np.abs(f(x_random))
puntos_in = puntos_in * np.sign(y_random) == np.sign(f(x_random))
puntos_in_positivo = puntos_in * (1 == np.sign(f(x_random)))
puntos_in_negativo = puntos_in * (-1 == np.sign(f(x_random)))

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in_positivo], y_random[puntos_in_positivo], color='green', s=10, label='In Postivo')
plt.scatter(x_random[puntos_in_negativo], y_random[puntos_in_negativo], color='goldenrod', s=10, label='In Negativo')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=10, label='Out')
plt.plot(x_values, f(x_values), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.axis('equal')
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo_positivo = (ymax - ymin) * (b - a) * np.sum(puntos_in_positivo) / N
integral_montecarlo_negativo = (ymax - ymin) * (b - a) * np.sum(puntos_in_negativo) / N
integral_montecarlo = integral_montecarlo_positivo - integral_montecarlo_negativo
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

c)  $$\int_\pi^{3\pi} \frac{\text{sen}(x)}{x}\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a = np.pi
b = 3*np.pi
f = lambda x: np.sin(x) / x
x_values = np.linspace(np.pi - 0.2, 3*np.pi + 0.2, 300)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x) = \frac{\sin(x)}{x}$')
plt.fill_between(np.linspace(a, b, 300), f(np.linspace(a, b, 300)), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.axhline(0, color='black', lw=1, ls='--')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: "center"

N = 100000
ymin = -0.35
ymax = 0.35
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y_random) <= np.abs(f(x_random))
puntos_in = puntos_in & (np.sign(y_random) == np.sign(f(x_random)))
puntos_in_positivo = puntos_in & (np.sign(f(x_random)) == 1)
puntos_in_negativo = puntos_in & (np.sign(f(x_random)) == -1)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in_positivo], y_random[puntos_in_positivo], color='green', s=5, label='In Positivo')
plt.scatter(x_random[puntos_in_negativo], y_random[puntos_in_negativo], color='goldenrod', s=5, label='In Negativo')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=5, label='Out')
plt.plot(np.linspace(a, b, 300), f(np.linspace(a, b, 300)), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo_positivo = (ymax - ymin) * (b - a) * np.sum(puntos_in_positivo) / N
integral_montecarlo_negativo = (ymax - ymin) * (b - a) * np.sum(puntos_in_negativo) / N
integral_montecarlo = integral_montecarlo_positivo - integral_montecarlo_negativo
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

d)  $$\int_0^\infty e^{-x^2} \cos(x)\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a = 0
b = np.inf
f = lambda x: np.exp(-x**2) * np.cos(x)
x_plot = np.linspace(-0.2, 5, 300)

plt.figure(figsize=(8, 5))
plt.plot(x_plot, f(x_plot), label=r'$f(x) = e^{-x^2}\cos(x)$')
plt.fill_between(np.linspace(0, 5, 300), f(np.linspace(0, 5, 300)), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.axhline(0, color='black', lw=1, ls='--')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo** (acotando en $[0, 10]$ ya que $e^{-x^2}$ tiende a 0 rápidamente):

```{python}
#| code-fold: true
#| fig-align: "center"

N = 500000
a_mc, b_mc = 0, 10
ymin, ymax = -1, 1
x_random = np.random.uniform(a_mc, b_mc, N)
y_random = np.random.uniform(ymin, ymax, N)

puntos_in = np.abs(y_random) <= np.abs(f(x_random))
puntos_in = puntos_in & (np.sign(y_random) == np.sign(f(x_random)))
puntos_in_positivo = puntos_in & (np.sign(f(x_random)) == 1)
puntos_in_negativo = puntos_in & (np.sign(f(x_random)) == -1)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in_positivo], y_random[puntos_in_positivo], color='green', s=3, label='In Positivo')
plt.scatter(x_random[puntos_in_negativo], y_random[puntos_in_negativo], color='goldenrod', s=3, label='In Negativo')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=3, label='Out')
plt.plot(np.linspace(a_mc, b_mc, 300), f(np.linspace(a_mc, b_mc, 300)), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo_positivo = (ymax - ymin) * (b_mc - a_mc) * np.sum(puntos_in_positivo) / N
integral_montecarlo_negativo = (ymax - ymin) * (b_mc - a_mc) * np.sum(puntos_in_negativo) / N
integral_montecarlo = integral_montecarlo_positivo - integral_montecarlo_negativo
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

e)  $$\int_0^1 x^x\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a, b = 0.0001, 1
f = lambda x: x**x
x_values = np.linspace(0.001, 1, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x) = x^x$')
plt.fill_between(x_values, f(x_values), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: "center"

N = 100000
ymin, ymax = 0, 1
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)
puntos_in = y_random <= f(x_random)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in], y_random[puntos_in], color='green', s=10, label='In')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=10, label='Out')
plt.plot(x_values, f(x_values), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo = (ymax - ymin) * (b - a) * np.sum(puntos_in) / N
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

f)  $$\int_1^5 e^{-x^2} x^3\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a, b = 1, 5
f = lambda x: np.exp(-x**2) * x**3
x_values = np.linspace(0.8, 5.2, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x) = e^{-x^2} x^3$')
plt.fill_between(np.linspace(a, b, 200), f(np.linspace(a, b, 200)), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: "center"

N = 100000
ymin, ymax = 0, f(1.5)
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)
puntos_in = y_random <= f(x_random)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in], y_random[puntos_in], color='green', s=10, label='In')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=10, label='Out')
plt.plot(np.linspace(a, b, 200), f(np.linspace(a, b, 200)), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo = (ymax - ymin) * (b - a) * np.sum(puntos_in) / N
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

g)  $$\int_0^\infty \frac{x}{e^x-1}\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a, b = 0.0001, np.inf
f = lambda x: x / (np.exp(x) - 1)
x_plot = np.linspace(0.01, 8, 300)

plt.figure(figsize=(8, 5))
plt.plot(x_plot, f(x_plot), label=r'$f(x) = \frac{x}{e^x - 1}$')
plt.fill_between(x_plot, f(x_plot), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, np.inf)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo** (acotando en $[0.001, 20]$):

```{python}
#| code-fold: true
#| fig-align: "center"

N = 500000
a_mc, b_mc = 0.001, 20
ymin, ymax = 0, 1
x_random = np.random.uniform(a_mc, b_mc, N)
y_random = np.random.uniform(ymin, ymax, N)
puntos_in = y_random <= f(x_random)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in], y_random[puntos_in], color='green', s=3, label='In')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=3, label='Out')
plt.plot(np.linspace(a_mc, b_mc, 300), f(np.linspace(a_mc, b_mc, 300)), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo = (ymax - ymin) * (b_mc - a_mc) * np.sum(puntos_in) / N
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

h)  $$\int_0^1 \frac{1}{\sqrt{x^4+1}}\,dx$$

```{python}
#| code-fold: true
#| fig-align: "center"

a, b = 0, 1
f = lambda x: 1 / np.sqrt(x**4 + 1)
x_values = np.linspace(-0.1, 1.1, 200)

plt.figure(figsize=(8, 5))
plt.plot(x_values, f(x_values), label=r'$f(x) = \frac{1}{\sqrt{x^4+1}}$')
plt.fill_between(np.linspace(a, b, 200), f(np.linspace(a, b, 200)), alpha=0.3, color='#333AAA')
plt.xlabel(r'$x$')
plt.ylabel(r'$f(x)$')
plt.legend()
plt.grid(True)
plt.show()
```

**Aproximación de la integral**

```{python}
aprox_integral, error_integral = integrate.quad(f, a, b)
print(f"La aproxiamción de la integral es: {aprox_integral}")
print(f"El error estimado es: {error_integral}")
```

**Método de Montecarlo**

```{python}
#| code-fold: true
#| fig-align: "center"

N = 100000
ymin, ymax = 0, 1
x_random = np.random.uniform(a, b, N)
y_random = np.random.uniform(ymin, ymax, N)
puntos_in = y_random <= f(x_random)

plt.figure(figsize=(8, 5))
plt.scatter(x_random[puntos_in], y_random[puntos_in], color='green', s=10, label='In')
plt.scatter(x_random[~puntos_in], y_random[~puntos_in], color='red', s=10, label='Out')
plt.plot(x_values, f(x_values), color='blue', label=r'$f(x)$')
plt.xlabel('x')
plt.ylabel('y')
plt.title('Método de Montecarlo para aproximar la integral')
plt.legend()
plt.grid()
plt.show()
```

```{python}
#| code-fold: true
integral_montecarlo = (ymax - ymin) * (b - a) * np.sum(puntos_in) / N
print(f"Aproximación de la integral con el método de Montecarlo:")
print(integral_montecarlo)
```

# Ejercicio 5

Aproximar las siguientes integrales dobles y triples, llevar a cabo la gráfica cuando se indique y comparar con el valor *exacto* de la integral.

a)  Realizar gráfica

$$\int_{-1}^{1}\int_1^2 (3y^2-x^2+5)\, dx\, dy$$

```{python}
#| code-fold: true
#| fig-align: "center"

x = np.linspace(1, 2, 50)
y = np.linspace(-1, 1, 50)
X, Y = np.meshgrid(x, y)
Z = 3*Y**2 - X**2 + 5

surface = go.Surface(x=X, y=Y, z=Z, colorscale='viridis', opacity=0.85)
base = go.Surface(x=X, y=Y, z=np.zeros_like(Z), colorscale='Blues', showscale=False, opacity=0.3)

fig = go.Figure(data=[surface, base])
fig.update_layout(title="f(x,y) = 3y² - x² + 5", scene=dict(
    xaxis_title="x", yaxis_title="y", zaxis_title="z"),
    width=800, height=500)
fig.show()
```

El valor exacto es $6$:

```{python}
#| code-fold: true

integral_exacta = 6
print(f"El valor exacto de la integral es: {integral_exacta}")

f5a = lambda y, x: 3*y**2 - x**2 + 5
integral_doble, error_doble = integrate.dblquad(f5a, 1, 2, -1, 1)
print(f"Aproximación con la función `integrate.dblquad` de `scipy`:")
print(integral_doble)
print("Error absoluto:", abs(integral_exacta - integral_doble))
print("Estimación del error absoluto proporcionada por `integrate.dblquad`:", error_doble)
```

b)  $$\int_{0}^{6}\int_1^5 \sqrt{x+4y}\, dx\, dy$$

```{python}
#| code-fold: true

f5b = lambda y, x: np.sqrt(x + 4*y)
integral_doble, error_doble = integrate.dblquad(f5b, 1, 5, 0, 6)
print(f"Aproximación con la función `integrate.dblquad` de `scipy`:")
print(integral_doble)
print("Estimación del error absoluto proporcionada por `integrate.dblquad`:", error_doble)
```

c)  $$\int_{1}^{e}\int_0^{\log(x)} x^3\, dy\, dx$$

```{python}
#| code-fold: true

f5c = lambda y, x: x**3
integral_doble, error_doble = integrate.dblquad(f5c, 1, np.e, 0, lambda x: np.log(x))
exacta_5c = 3*np.e**4/16 + 1/16
print(f"El valor exacto de la integral es: {exacta_5c:.8f}")
print(f"Aproximación con la función `integrate.dblquad` de `scipy`:")
print(integral_doble)
print("Error absoluto:", abs(exacta_5c - integral_doble))
print("Estimación del error absoluto proporcionada por `integrate.dblquad`:", error_doble)
```

d)  $$\iint_D 30ye^x\, dx\, dy$$

Donde $D\subset \mathbb{R}^2$ es la región de la gráfica.

```{python}
#| code-fold: true
#| fig-align: 'center'

x_val = np.array([0,4])
y_val1 = np.array([0, 1])
y_val2 = np.array([0, 4])

plt.figure(figsize=(8,6))
plt.plot(x_val, y_val1)
plt.plot(x_val, y_val2)
plt.fill_between(x_val, y1=y_val1, y2=y_val2, color="firebrick", alpha=0.5)
plt.grid()
plt.show()
```

```{python}
#| code-fold: true

f5d = lambda y, x: 30 * y * np.exp(x)
integral_doble, error_doble = integrate.dblquad(f5d, 0, 4, lambda x: x/4, lambda x: x)
print(f"Aproximación con la función `integrate.dblquad` de `scipy`:")
print(integral_doble)
print("Estimación del error absoluto proporcionada por `integrate.dblquad`:", error_doble)
```

e)  $$\iiint_B z\, e^{x+y}\, dx\, dy\, dz, \quad B=[0,1]\times[0,1]\times[0,1]$$

```{python}
#| code-fold: true

f5e = lambda z, y, x: z * np.exp(x + y)
integral_triple, error_triple = integrate.tplquad(f5e, 0, 1, 0, 1, 0, 1)
exacta_5e = 0.5 * (np.e - 1)**2
print(f"El valor exacto de la integral es: {exacta_5e:.8f}")
print(f"Aproximación con la función `integrate.tplquad` de `scipy`:")
print(integral_triple)
print("Error absoluto:", abs(exacta_5e - integral_triple))
print("Estimación del error absoluto proporcionada por `integrate.tplquad`:", error_triple)
```

f)  $$\int_0^1\int_0^x\int_0^y (y+xz)\, dz\, dy\, dx$$

```{python}
#| code-fold: true

f5f = lambda z, y, x: y + x*z
integral_triple, error_triple = integrate.tplquad(f5f, 0, 1, 0, lambda x: x, 0, lambda x, y: y)
exacta_5f = 3/28
print(f"El valor exacto de la integral es: {exacta_5f:.8f}")
print(f"Aproximación con la función `integrate.tplquad` de `scipy`:")
print(integral_triple)
print("Error absoluto:", abs(exacta_5f - integral_triple))
print("Estimación del error absoluto proporcionada por `integrate.tplquad`:", error_triple)
```

# Ejercicio 6

De `scipy.stats` elige 5 distribuciones continuas diferentes de probabilidad continua, realiza la gráfica y encuentra la probabilidad que la variable aleatoria tome un valor en un intervalo dado. Compara el resultado con el método `cdf`.

```{python}
#| code-fold: true
#| fig-align: "center"

distribuciones = [
    {"nombre": "Normal — N(0,1)", "dist": stats.norm(loc=0, scale=1),
     "x_range": (-4, 4), "a": -1, "b": 1},
    {"nombre": "Exponencial — Exp(λ=1)", "dist": stats.expon(scale=1),
     "x_range": (0, 6), "a": 0, "b": 2},
    {"nombre": "Gamma — Γ(α=3, β=1)", "dist": stats.gamma(a=3, scale=1),
     "x_range": (0, 10), "a": 1, "b": 5},
    {"nombre": "Beta — Beta(α=2, β=5)", "dist": stats.beta(a=2, b=5),
     "x_range": (0, 1), "a": 0.2, "b": 0.7},
    {"nombre": "Chi-cuadrada — χ²(k=4)", "dist": stats.chi2(df=4),
     "x_range": (0, 14), "a": 1, "b": 6}
]

for d in distribuciones:
    dist = d["dist"]
    a_i, b_i = d["a"], d["b"]
    x_vals = np.linspace(*d["x_range"], 300)

    prob_quad, _ = integrate.quad(dist.pdf, a_i, b_i)
    prob_cdf = dist.cdf(b_i) - dist.cdf(a_i)

    plt.figure(figsize=(8, 4))
    plt.plot(x_vals, dist.pdf(x_vals), color='steelblue', linewidth=2, label='PDF')
    plt.fill_between(np.linspace(a_i, b_i, 200), dist.pdf(np.linspace(a_i, b_i, 200)),
                     alpha=0.4, color='steelblue',
                     label=f'P({a_i} ≤ X ≤ {b_i}) = {prob_quad:.4f}')
    plt.title(d["nombre"])
    plt.xlabel("x")
    plt.ylabel("f(x)")
    plt.legend()
    plt.grid(alpha=0.3)
    plt.tight_layout()
    plt.show()

    print(f"{d['nombre']}")
    print(f"  P({a_i} ≤ X ≤ {b_i}) vía integrate.quad : {prob_quad:.6f}")
    print(f"  P({a_i} ≤ X ≤ {b_i}) vía CDF            : {prob_cdf:.6f}")
    print(f"  Diferencia                               : {abs(prob_quad - prob_cdf):.2e}\n")
```